{
  "name": "edentic-v3",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "tauri:dev": "node scripts/tauri-dev.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test",
    "build:frontend": "node scripts/build-frontend.mjs",
    "build": "npm run build:frontend && cargo build --manifest-path src-tauri/Cargo.toml --release"
  },
  "engines": {
    "node": ">=20"
  }
}
